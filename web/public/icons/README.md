# Home-screen / app icons (per theme)

Drop one PNG per theme here, named exactly (square, 180×180 or larger — 1024 is fine):

  library.png   midnight.png   slate.png   forest.png   sepia.png   paper.png

Used as the iOS "Add to Home Screen" icon (apple-touch-icon). The active theme's
icon is selected at the moment the user adds the page to their home screen.
